<!-- # To-do

* When response is too long it stops outputting for some reason
* Need to implement markdown and latex formatting while streaming.
* Implement better prompt engineering and RAG according to user needs (specific subject, AP class, SAT, academy, etc.)
* Incorporate NoteBookLM features

# UI Deisgn To Do

* Questions.html. Make it so that when at question 1, you the previous question button does not show up since there is not previous question and also for the next button when there is no next question to either make the button not show up or make it take the user back to question bank. --> Dont do this

Do Not do the things commented out.

# Sidebar 
* Update the emojis to be svg icons instead.
* Make the sat dropdown css look better and match the flow of the sidebar. Also, make it into a button and not let it have the like arrow for the dropdown in it. 
* Add a collpased "more" icon when the sidebar is collpased like in the question bank "more" icon but only with options like in the question bank one to change mode (dark/light mode), then the other options like settings icon, so if they click it it opens the setting modal and also the logout icon. The log out icon should be a .danger.

# productivity.html
* Make the buttons that have a green background, purple. 

# questions.html
<!-- * Add demo data when if using local machine. Then for "Question X out of Y" add like a big modal or something similar where it shows the user all the questions they chose, if they click on the question number it takes them to it and it shows which ones they got right or wrong or marked for review.  --> Dont do this
* Make navbar bigger when window is full size. 
* Make all the text in dark mode white no matter what.
* Make the collapse at a breakpoint, be like the sat math mode's. For example, desmos on top and question on the bottom, in two rows and the user can change the height (the resize handle) and make it match the flow and colors of the quesiton bank in both light and dark mode. 
<!-- * Make it more like oneprep quesiton bank --> Dont do this
* Fix the reference sheet drag, (its not working and not draggable at all). 
