<!-- # To-do

* When response is too long it stops outputting for some reason
* Need to implement markdown and latex formatting while streaming.
* Implement better prompt engineering and RAG according to user needs (specific subject, AP class, SAT, academy, etc.)
* Incorporate NoteBookLM features

# UI Deisgn To Do

* Questions.html. Make it so that when at question 1, you the previous question button does not show up since there is not previous question and also for the next button when there is no next question to either make the button not show up or make it take the user back to question bank. --> Dont do this

Do not do the things commented out.
Do everything that is not commented out.
PLAN EVERY STEP OF EVERYTHING YOUR GOING TO DO AND MAKE A TABLE FOR IT FOR ME
I DO NOT WANT MISTAKES. I WANT YOU TO BE SURE OF EVERY CHANGE AND VERIFY THEM COMPLETELY.
MAKE SURE AS YOU PLAN THAT EVERY CHANGE FOLLOWS UI-UX.md AND ASK QUESTIONS.

# Sidebar 
* Make logout icon when sidebar is full icon red. 

# productivity.html
* Make the buttons that have a green background, purple.  (Relevant html: button class = "timer-btn t-btn" and button class ="t-btn text-xs font-bold" and button class = "todo-add-btn t-btn")

# questions.html
<!-- * Add demo data when if using local machine. Then for "Question X out of Y" add like a big modal or something similar where it shows the user all the questions they chose, if they click on the question number it takes them to it and it shows which ones they got right or wrong or marked for review.  --> Dont do this
* Make navbar bigger when window is full size. Just increase the height pretty much.
* Make all the text white in dark mode.
* Make the collapse at a breakpoint, be like the sat math mode's. For example, desmos on top and question on the bottom, in two rows and the user can change the height (the resize handle) and make it match the flow and colors of the quesiton bank in both light and dark mode. (../sat/math-chat.html) (Relevant html: div class = "resize-handle" and div=sat-math-split)
<!-- * Make it more like oneprep quesiton bank --> Dont do this
* Fix the reference sheet drag, (its not working and not draggable at all). (Relevant html:  button id="referenceBtn" class = "sat-button")

******************************************NEWER UPDATES I WANT TO DO************************************************ DO NOT DO THESE.
# questions.html
* Fix the structure of the questions, like, making the question container and desmos graph perfectly fit the container when possible and if not, make a scroll.
* Fix the demsos loading, because when the page is loaded, you have to click like a answer option in order to it be full size, if not it's like half its actual size. 
* Theres like an invisible footer like this at the bottom of the main content area.

# math-chat.html
* Add the pomodoro timer to the sidebar (It's not there for some odd reason).
* Add like a file icon or like a + icon (material icon svg) and put it like the opposite side of the send button, so to the right and under the chat input area.  (Maybe incorporate the document panel in here but no tab, just the icon for the document panel and when clicked, the panel comes up)


# Sidebar
* Update the icons for the recent chats, like each one. They shouldn't be emojis but rather svg icons using material icons.
* Make the text size of the 2 sat links dropdown smaller and incorperative nice, smooth interactive hovers and a nice dropdown animation when the sat button is clicked to make things look smooth. And add a hover, a purple one in index.html where the left border like in the other ones gets highlighted purple. More like the hovers for the "more" icon dropdown. 

# Index.html
* Fix the panel and sidebar problem where if one if open and then another is clicked, the other is closed and the one clicked it open. It should work across ALL breakpoints.



